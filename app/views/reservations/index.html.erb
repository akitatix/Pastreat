<div class="ordre">
  <div>
    <% @reservations.each do |x| %>

      <div class="reservation_principale">
        <div class="main">
          <h2><%= x.boulangerie.name  %> </h2>
          <p><%= x.date %></p>
          <p> <%= x.price %> </p>
          <p> <%= x.nb_pc %> pain(s) au chocolat </p>
          <p> <%= x.nb_cr %> croissant(s) </p>
        </div>

        <div class="recapitulatif" >

        </div>
      </div>
    <% end %>
  </div>
</div>
